{
  "name": "game-of-the-year---grupp-4",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "api": "json-server --watch db.json -p 3000",
    "ts-w": "tsc --watch --noEmit"
  },
  "devDependencies": {
    "@types/p5": "^1.7.7",
    "typescript": "~6.0.2",
    "vite": "^8.0.4"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.15",
    "p5": "^2.2.3",
    "sass": "^1.99.0"
  }
}
